import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "SupplyConnect - Connect Street Food Vendors with Suppliers",
  description: "A platform connecting street food vendors with trusted raw material suppliers across India",
  keywords: "street food, suppliers, vendors, raw materials, India, food supply chain",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
